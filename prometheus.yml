global:
  scrape_interval: 10s # 10초마다 지표 수집
  evaluation_interval: 10s

scrape_configs:
  - job_name: 'harudang-backend'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['app:8080'] # docker-compose 내 서비스 이름인 'app'